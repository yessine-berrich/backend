export enum userRole {
  ADMIN = 'ADMIN',
  EMPLOYEE = 'EMPLOYEE',
}

export enum ArticleStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  PENDING = 'pending',
  ARCHIVED = 'archived',
  REJECTED = 'rejected',
}

export enum NotificationType {
  MENTION = 'mention',
  REPLY = 'reply',
}